# Javascript Node CircleCI 2.0 configuration file
#
# Check https://circleci.com/docs/2.0/language-javascript/ for more details
#
version: 2
jobs:
  build:
    machine: true
    working_directory: ~/repo

    steps:
      - checkout

      - run:
          name: Build Docker Image
          command: docker build -t bk-nexus .

      - run: 
          name: Push To Heroku
          command: |
            docker login --username=katnich@shaw.ca --password=c42504b5-be69-467e-a86b-2959b66bb45e registry.heroku.com
            docker tag bk-nexus registry.heroku.com/bk-nexus/web
            docker push registry.heroku.com/bk-nexus/web:latest

      - run: 
          name: Deploy on Heroku 
          command: |
            npm install heroku
            heroku container:release registry.heroku.com/bk-nexus/web -a bk-nexus      
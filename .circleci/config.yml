# Javascript Node CircleCI 2.0 configuration file
#
# Check https://circleci.com/docs/2.0/language-javascript/ for more details
#
version: 2
jobs:
  build:
    machine:
      services:
        - docker

    dependencies:
      cache_directories:
        - ~/docker

    deployment:
      staging:
        branch: master
        commands: |
          - docker login --email=katnich@shaw.ca --username=$katnich@shaw.ca --password=$c42504b5-be69-467e-a86b-2959b66bb45e registry.heroku.com
          - docker build --rm=false -t registry.heroku.com/bk-nexus/web .
          - docker push registry.heroku.com/bk-nexus/web:latest